//
// Created by anon on 1/3/22.
//

#include "CsvException.h"

CsvParser::CsvParser(int column_count, char delimiter):  column_count(column_count), delimiter(delimiter), column_names(new string[column_count]) {
    for(int i=0; )
}